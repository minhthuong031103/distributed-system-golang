apiVersion: v1
kind: ConfigMap
metadata:
  name: consul-config-b
data:
  consul-config.json: |
    {
      "service": {
        "name": "service-b",
        "tags": ["urlprefix-/service-b"],
        "port": 8080
      }
    }
